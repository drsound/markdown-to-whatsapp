```
docker run --rm -it -v "$PWD":/app -w /app node:22 npm run build -- --verbose
https://example.com/a/very/long/path/that/never/breaks/anywhere?token=abcdef0123456789

function demo() {
    if (ready) {
        return "indented, and kept that way";
    }
}
```
